# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
.tmp-ios-bundle/

# Native
# Prebuild output — do not commit. Run `npx expo prebuild` locally when debugging native code;
# EAS Build generates native projects on the server for CI/production builds.
/ios/
/android/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*
.cursor/
firestore-debug.log
scripts/__pycache__/

# macOS
.DS_Store
*.pem

# Firebase native config (inject locally or via EAS secrets — see scripts/setup-eas-firebase-files.mjs)
GoogleService-Info.plist
assets/GoogleService-Info.plist
assets/google-services.json

# local env files
.env
.env*.local

# Firebase Admin service account JSON (local only; use FIREBASE_SERVICE_ACCOUNT_PATH)
**/serviceAccountKey.json
functions/**/serviceAccount*.json
# Console "Generate new private key" downloads (e.g. *-firebase-adminsdk-*.json)
*-firebase-adminsdk-*.json

# Firebase Apple SDK manual zip (local only; iOS uses SPM via prebuild plugin)
/Firebase/

# typescript
*.tsbuildinfo

app-example

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# local python venvs (docx generator)
.venv-handoff/
.env.local
