# Deployed with firebase deploy for project into-the-pond (Path A).
# Non-secret runtime flags only — safe to commit.
#
# ACCOUNT_DELETION_SECRET / RESEND_API_KEY must NEVER appear in this committed file.
# - Local scripts: set secrets in gitignored functions/.env.local
# - Deploy ritual: temporarily add secrets + ACCOUNT_DELETION_ENABLED=true
#   to this file, deploy, then scrub before committing.
#   See functions/.env.example and docs/firebase-hosting-delete-account.md.
ECONOMY_LEDGER_ONLY=true
ACCOUNT_DELETION_ENABLED=false
