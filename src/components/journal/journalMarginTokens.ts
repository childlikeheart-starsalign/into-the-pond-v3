/** Visual tokens for JournalMargin — closed-scope book furniture. */

/** Bark Umber — structural margin ink; never tier-coded. */
export const JOURNAL_MARGIN_INK = "#7A5C45";

/** Uniform opacity across every surface. */
export const JOURNAL_MARGIN_OPACITY = 0.55;

export const JOURNAL_MARGIN_COLOR = `rgba(122, 92, 69, ${JOURNAL_MARGIN_OPACITY})`;

export const JOURNAL_MARGIN_FONT_SIZE = 10;
export const JOURNAL_MARGIN_CHAPTER_FONT_SIZE = 11;
export const JOURNAL_MARGIN_LEFT_LETTER_SPACING = 1.2;

export const JOURNAL_MARGIN_LEFT_ANCHOR = 4;
/** Surface 1 cast-finish — nudge toward card edge. */
export const FIELD_ENTRY_LEFT_OFFSET = -15;
