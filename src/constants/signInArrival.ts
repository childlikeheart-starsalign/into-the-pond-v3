/** Minimum time on 24.png before transitioning to the waiting video. */
export const SIGN_IN_ARTBOARD_MIN_MS = 600;

/** Minimum time from arrival start before navigating to destination. */
export const SIGN_IN_ARRIVAL_MIN_MS = 2000;

/** Overlay opacity fade duration after navigation. */
export const SIGN_IN_ARRIVAL_FADE_MS = 400;
