import { ILLUSTRATED_TAB_BAR_HEIGHT } from "@/src/constants/illustratedTabBar";
import { spacing } from "@/src/constants/theme";

/** Bottom inset so scroll content clears the illustrated tab bar overlay. */
export const TAB_SCREEN_BOTTOM_PADDING = ILLUSTRATED_TAB_BAR_HEIGHT + spacing.inner;
