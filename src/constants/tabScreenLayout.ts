import { SANCTUARY_TAB_BAR_CLEARANCE_PX } from "@/src/constants/sanctuaryNavLayout";
import { spacing } from "@/src/constants/theme";

/** Bottom inset so scroll content clears the illustrated tab bar overlay. */
export const TAB_SCREEN_BOTTOM_PADDING = SANCTUARY_TAB_BAR_CLEARANCE_PX + spacing.inner;
